/*
The following line does not work, thus please include the static CSC template
in the template record.
*/
# <INCLUDE_TYPOSCRIPT: source="FILE:EXT:css_styled_content/static/constants.txt">

styles.content.uploads.filesizeBytesLabels = " Byte| KB| MB| GB"

# Target for links
styles.content.links.extTarget = _blank
styles.content.links.fileTarget = _blank

# Responsive Image Rendering
styles.content.imgtext.layoutKey = picturefill